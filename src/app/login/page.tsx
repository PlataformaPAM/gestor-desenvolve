import { LoginExperience } from "@/components/auth/login-experience";

/**
 * Login centralizado (CPF + senha). Cookie gestor_session + redirect para Central de Comandos.
 */
export default function LoginPage() {
  return <LoginExperience />;
}
